# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html

parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        #autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: true
    
    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    kiraxe\AdminCrmBundle\:
        resource: '../../src/kiraxe/AdminCrmBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/kiraxe/AdminCrmBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    kiraxe\AdminCrmBundle\Controller\:
        autowire: true
        resource: '../../src/kiraxe/AdminCrmBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    kiraxe\AdminCrmBundle\Entity\:
        autowire: true
        resource: '../../src/kiraxe/AdminCrmBundle/Entity'
        public: true

    kiraxe\AdminCrmBundle\Controller\SecurityController:
        autowire: true
        public: true
        bind:
            $authenticationUtils: '@security.authentication_utils'

    kiraxe\AdminCrmBundle\Services\Dump\DumpCommand:
        autowire: true
        arguments:
            $progect_dir: '%kernel.project_dir%'
    #kiraxe\AdminCrmBundle\Services\DumpDbs:
    #    autowire: true
    #    arguments:
    #        $host: '%database_host%'
    #        $port: '%database_port%'
    #        $dbname: '%database_name%'
    #        $user: '%database_user%'
    #        $password: '%database_password%'
    #        $charset: UTF8


    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'
